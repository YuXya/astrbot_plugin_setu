# AstrBot 涩图插件

Astrbot 涩图插件，支持自定义配置与标签指定

## 使用方法

- `/setu get` 获取一张随机涩图
- `/setu get <tag>` 获取特定标签的随机涩图
- `/setu details` 查看上一张涩图的详细信息
- `/setu help` 查看帮助

普通用户的 `/setu get`、`/setu r18` 与 AI 工具调用共享 60 秒冷却，
每次只查找并发送一张图片；管理员调用次数不受限制。
